// 플러그인 리포지토리 설정
pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'app'

// 의존성 모듈 포함 (의존성 해결을 위해 필요)
includeBuild('../auth-module')
includeBuild('../common') 